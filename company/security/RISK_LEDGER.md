# リスク台帳 — セキュリティ対策室(鵜飼 静)

修正した穴と「知った上で受容したリスク」をすべて記録する。黙って放置しない。
週次(月曜定例)でCodex監査を実施し、結果をここに追記する。

## 監査履歴

| 回 | 日付 | 指摘 | 対応 |
|---|---|---|---|
| 第1回 | 2026-07-21 | 16件(重大5) — JSON破損連鎖・XSS(JSON-LD)・デプロイ黙殺ほか | 13件修正、3件は第3回までに消化 |
| 第2回 | 2026-07-22 | 11件(重大2) — いいね経由Stored XSS・ランキング改ざんほか | 9件修正(台帳照合方式へ全面改修) |
| 第3回 | 2026-07-23 | 9件(重大4) — 週刊生成クラッシュ・投資助言ガード不在ほか | 8件修正(助言の機械検査・push競合リトライ等) |
| 第4回 | 2026-07-23 | 10件(**重大ゼロ**・中6) — PV日付ズレ・viewsルール甘さ・日本企業欄への海外混入ほか | 9件修正。残1件(アーカイブ孤児ページ)は影響軽微で次回持越し。監査官所見「明確なリグレッションなし」 |
| 第5回 | 2026-07-25 | 9件(重大1・中4) — **slug衝突で用語ページが実際に消失**(Nvidia/NVIDIA、サカナAI/ai&、GPT/チャットGPT)ほか | **9件すべて修正済み**(同日中にOGPアトミック保存+壊れ画像の再生成も対応)。重大は一意化の二重防御 |

| 第6回 | 2026-07-25 | 6件(重大1) — **孤児掃除が台帳読込失敗時に619ファイルを全削除しうる**(自作機能が最大の凶器化)ほか | **6件すべて修正**。安全弁(空データ/3割超削除で中止)を危険3ケースで実証 |
| 第7回 | 2026-07-25 | 6件(**重大ゼロ**・中3) — バズ画像の失敗が不可視/カード無期限増加/部分取得でも「6地域」と断定表示 | **6件すべて修正**。以後**実装のたびにレビュー必須**を規程化(社長指示) |
| 第8回 | 2026-07-25 | 7件(重大3) — voices生成停止の実装。**mark_postedを保存前に呼ぶ改悪で記事を恒久的に取りこぼす**/台帳が読めない時に上書きしうる/重大警報が警告止まり ほか | **7件すべて修正**。重大1は当日私が入れたリグレッション(保存後に移動) |
| 第9回 | 2026-07-25 | 3件(重大1) — **台帳が正当な空になると、記事ゼロのサイトで全ページを上書きしてデプロイしうる**/precheckが壊れた台帳で警報を返す前に例外死/mark_posted失敗時の二台帳不整合 | **3件すべて修正**。`build.LedgerError`で便を中止する設計に変更 |

※**過去6回の指摘61件はすべて処理完了**(意図的な受容判断5件を除く)。
**教訓(第6回)**: 「掃除・削除する機能」は、入力が壊れたときに最大の破壊力を持つ。実装時は必ずフェイルクローズ(疑わしきは何もしない)と、異常量の検知を入れる。

## 受容済みリスク(社長了承ベース)

| # | リスク | 受容理由 | 条件・見直し時期 |
|---|---|---|---|
| R1 | いいねの連打・水増しは完全には防げない | 完全防御には利用者認証/App Checkが必要で個人サイトには過剰。偽記事表示は台帳照合で不可能、課金被害なし(無料枠ハードキャップ)を実測確認済み | PVが伸びて広告収益が付いたら再検討 |
| R2 | APIキーが平文.envでPC内に存在(Gemini/YouTube/X) | ローカルPC完結の設計のため。リポジトリ・チャットへは書かない運用を徹底 | PC共用化・外部公開時は即時に金庫化を検討。漏えい時は各サービスで再発行 |
| R3 | 編集部ライブの「実行中」表示が長い便で早めに待機に戻る | 見た目のみの問題 | 苦情が出たら |
| R4 | Firestore書込クォータをスパムで消費される可能性 | 無料枠の日次上限で自動停止、課金なし。サイト表示(読み)は影響を受けにくい | 発生を検知したらルール強化 |
| R5 | 自作PVカウンター(views)は水増し可能(+1のみだが連打可) | いいね(R1)と同じ受容判断。数字は「傾向を見る」用途に限定し、絶対値を成果指標にしない。+5等の飛ばしは拒否を実測確認済(07-23) | GA4導入後は参考値に格下げ |
| **R6** | **公開中のvoices記事10本(識者ブログの翻訳・紹介)を残す**(2026-07-25 社長裁可・案A) | **新規生成は停止済みで出血は止まっている**ため、いま急いで消す実益がない。全記事に出典リンクを明記済み。削除は後戻りできず、慌てて判断する場面ではない(2026-07-25 緊急役員会D1) | **①著作者本人または代理人から申し出があれば即時に非公開**(最優先で対応する) ②D2「事実抽出→別工程で執筆」への移行時に、全カテゴリの過去記事とあわせて扱いを再検討 ③月次で件数と状態を確認 |

## インシデント記録

| 日付 | 事象 | 原因 | 恒久対策 |
|---|---|---|---|
| 07-21 | タスク0x1即死(2回) | ps1内の日本語コメント(ANSI解釈) | 純ASCII鉄則+同条件実走テスト |
| 07-21 | 本番ログ喪失 | 監視tail -fの孤児プロセスがログを書込ロック | 監視はgitポーリング方式に全面変更 |
| 07-21〜22 | タスク実行時のみデプロイ失敗(3回) | サンドボックスACLでnpm配下がタスクから不可視 | firebase-toolsをプロジェクト内複製+多段フォールバック |
| 07-22 | AI見出しが釣り文句化(13案) | 心理学ルールの副作用 | 禁止ワードリスト+検品で阻止(公開前に発見) |
| 07-21 | 人物カードに別人(トランプ→カードゲーム) | 同名項目の誤マッチ | Wikipedia候補5件と紹介文の内容照合方式(公開前に発見) |

## 新機能セキュリティチェックリスト(着工前に必ず)

1. **外部由来の文字列は信用しない** — RSS・LLM出力・API応答・DBの値は、表示時にtextContent/エスケープ。innerHTML連結は禁止
2. **書き込み可能データは三重で縛る** — ルールで形式・長さ・遷移を制限 / 表示側は信頼台帳と照合 / 攻撃してみて拒否を実測
3. **鍵は運ばない** — コード・リポジトリ・チャット・コミットに書かない。読み込みは実行時にローカル.envから
4. **スクリプトは純ASCII**+タスクと同条件で実走してから配置
5. **法務系(投資助言・医療・誹謗)はプロンプト頼み禁止** — 機械検査を併設し、検出時は公開せず破棄。**検査対象は表示面の全フィールド**(タイトルだけ見て3行まとめを見ない、を禁ずる)
6. **日時計算はJST固定を実測確認**(閲覧者タイムゾーン依存の計算は禁止。0時台の挙動を必ず検証)
7. **外部から書けるデータは全フィールドの型・サイズ・遷移を検証**(totalだけ見てpagesを見ない、を禁ずる)
8. **削除・上書きを伴う機能は着工前に鵜飼へ連絡し、フェイルクローズで作る**(第6回事故)
   - 入力が空/読めない場合は「何もしない」(空と読込失敗は区別できない前提で設計)
   - 想定を超える量(既存の3割超)を消そうとしたら中止する
   - 対象を生成物の拡張子・命名規則に限定する
   - **危険入力(空・1件だけ・型不正)を実際に流して、止まることを実証してから完了とする**
9. **入力の権利を確認する**(2026-07-25 緊急役員会。それまでチェックリストは技術的安全のみで、著作権の項目がゼロだった)
   - 新しいソースを足す前に **robots.txt と RSS/サイトの利用規約**を読む(例: Yahoo!ニュースのRSSは
     「RSSから取得した情報を用いたウェブサイトの公開」を明示的に禁止している)
   - **他社の記事本文をLLMに渡して要約させない**。要約は翻案に当たり得る(日本新聞協会の公式見解)。
     事実だけを抽出し、別工程で書く
   - **翻訳は特に危険**。個人の評論・エッセイは報道記事より著作物性が明確で、翻訳は翻訳権(27条)に直撃する。
     CC等の許諾表示があるかを必ず確認する(表示がなければ全権利留保)
   - 一次情報(適時開示・官公庁発表・統計・公式ブログ・自社集計)を入力にできないかを先に検討する
10. **台帳を読めないことと、台帳が空であることを区別する**(第9回)
   - `build._check_ledger()` を通す。公開済みページがあるのに台帳が空なら**読込失敗とみなして中止**
   - 本当にゼロから始めるときは `data/articles.json` を**削除**する(空の`[]`を置かない)

## 自己評価メモ(2026-07-23 鵜飼)

- 46指摘の内訳: 同型再発5件(私の網羅性不足)/新機能固有の初物31件/環境固有の罠10件
- **読者への実害ゼロ・短時間公開2件のみ** — 「ミスが客に届く前に死ぬ」仕組みは機能している
- 教訓: チェックリストは事故のたびに項目を増やす生き物として扱う
